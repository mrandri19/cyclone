name: bike-rental-regression

docker_env:
  image: mlflow-docker-example

entry_points:
  main:
    parameters:
      num_train: { type: int }
      num_test: { type: int }
    command: "python train.py {num_train} {num_test}"
